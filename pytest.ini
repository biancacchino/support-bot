[pytest]
# auto, so async fixtures work under a plain @pytest.fixture. The explicit
# @pytest.mark.asyncio markers are kept anyway: they say what a test is without
# making the reader go and check the mode.
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function

markers =
    slow: loads real model weights and embeds the corpus (seconds, not milliseconds)
